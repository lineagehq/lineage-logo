# Preparation receipt — U1 kit only

- Prepared from `c410d08aab724877908fcf9efa687b3b5edec9b3` in the isolated `codex/logo-usability-study` worktree.
- Scope: this directory only; no application, graph, runtime, package, CI or public-registry protocol changes.
- Grounding: plan v1 U1 criteria, final technical oracle and existing public-beta cohort protocol. Eight prompts and assessor oracles cover every required human task, state comprehension, result discovery and integrity preservation.
- Validation: all JSON files parse; all relative Markdown link targets exist; starting fixture SHA-256 recomputed; `git diff --check` clean. Ajv 8.20.0 strict draft-07 schema compilation and incomplete template validation pass. Five negative cases reject: extra narrative field, out-of-bound duration, malformed digest, unrecognized assistance code and falsely marking the empty template completed. Existing installed Ajv was read from the inspector worktree; no dependencies or runtime code changed.
- Limitation: schema validates controlled shape and completion fields, not observed truth, distinct humans or artifact content. Aggregate and candidate files are explicit manual preflight templates, not automated verifiers.
- Original preparation remaining work is superseded by the update below. No participant attempt occurred and no U1 pass is claimed.

## Deterministic assets and rehearsal update

The kit now contains public SVG proposal/correction assets, participant briefs, a public-CLI producer and an installed-package maintainer rehearsal. Structural submissions require the unchanged starting fixture digest so the fixed artifact cannot overwrite intervening manual changes. Follow-up changes only the orbit fill from the current handoff.

Seven retained non-counting receipts distinguish setup failures, published beta.4’s cross-parent alignment blocker, and a successful unpublished local candidate. The successful rehearsal covers all eight technical milestones, including independent XML/render checks and process restart; it does not establish human comprehension or usability. See [rehearsal evidence](rehearsal-evidence/README.md).

Remaining: requalify merged alignment fix `d669ebef20bdf8bbd3a1705094a22be3b6809d7a`, freeze the final candidate and shipped instructions with renewed applicable G4 evidence, confirm user-arranged participants and local handling, and collect three actual independent passing attempts. Published beta.4 remains explicitly blocked for this study. VoiceOver remains deferred by the user.
