# Preparation receipt — U1 kit only

- Prepared from `c410d08aab724877908fcf9efa687b3b5edec9b3` in the isolated `codex/logo-usability-study` worktree.
- Scope: this directory only; no application, graph, runtime, package, CI or public-registry protocol changes.
- Grounding: plan v1 U1 criteria, final technical oracle and existing public-beta cohort protocol. Eight prompts and assessor oracles cover every required human task, state comprehension, result discovery and integrity preservation.
- Validation: all JSON files parse; all relative Markdown link targets exist; starting fixture SHA-256 recomputed; `git diff --check` clean. Ajv 8.20.0 strict draft-07 schema compilation and incomplete template validation pass. Five negative cases reject: extra narrative field, out-of-bound duration, malformed digest, unrecognized assistance code and falsely marking the empty template completed. Existing installed Ajv was read from the inspector worktree; no dependencies or runtime code changed.
- Limitation: schema validates controlled shape and completion fields, not observed truth, distinct humans or artifact content. Aggregate and candidate files are explicit manual preflight templates, not automated verifiers.
- Remaining: G4-ready candidate/package/producer assets and all final hashes; non-counting installed rehearsal; user-arranged participants and approved local handling; three real independent passing attempts; retained failed receipts and resolved blockers. No participant attempt occurred and no U1 pass is claimed.
