# Agent brief

Add a gold diamond above the Seatify wordmark while preserving the existing seats, table, text and named mark/wordmark groups.

The prepared first draft deliberately makes the diamond too large. Inspect it and request: **Make the gold diamond smaller, keeping everything else unchanged.** The corrected proposal reduces its half-width and half-height from 60 to 18 document units.
